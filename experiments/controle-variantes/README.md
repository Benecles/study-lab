# Controle de Constitucionalidade: rejected style variants (2026-09-23)

Kept for reference. The owner chose the original drafting-sheet lesson (`courses/controle-de-constitucionalidade/aula-11.html`, full graph-paper background) over all of these:

- `alternativas-estruturais.html`: five structural metaphors (court files, metro map, front page, board game, overhead transparencies).
- `temas.css`: five Japanese-pastel / riso themes (sakura, washi, yūgure, himawari, soda). Activated by `html[data-theme]`.
- `fundos.css`: six background treatments (suave, pontos, margem, foco, prancheta, vegetal). Activated by `html[data-fundo]`.
- `pickers.js`: the query-string switchers (`?tema=`, `?fundo=`) used to preview them on the live lesson.

To preview again: link both CSS files and the picker script from a copy of aula-11.html.

Owner's words: the original "still uses more of the paper motifs". He also found the full graph-paper background "a bit too strong", yet still preferred it over every softer background.
