/* Optional reading preferences; content and navigation work without JavaScript. */
(()=>{const prose=document.querySelector('.prose');if(!prose)return;const controls=document.createElement('div');controls.className='reading-tools';controls.setAttribute('aria-label','Tamanho do texto');const smaller=document.createElement('button'),larger=document.createElement('button'),status=document.createElement('output');smaller.textContent='A−';larger.textContent='A+';smaller.setAttribute('aria-label','Diminuir texto');larger.setAttribute('aria-label','Aumentar texto');status.setAttribute('aria-live','polite');let scale=1;try{const n=Number(localStorage.getItem('study-lab-text-scale'));if(n>=.9&&n<=1.3)scale=n}catch{}const base=parseFloat(getComputedStyle(prose).fontSize);function apply(){prose.style.fontSize=(base*scale)+'px';status.textContent=Math.round(scale*100)+'%';smaller.disabled=scale<=.9;larger.disabled=scale>=1.3;try{localStorage.setItem('study-lab-text-scale',String(scale))}catch{}}smaller.addEventListener('click',()=>{scale=Math.max(.9,Math.round((scale-.1)*10)/10);apply()});larger.addEventListener('click',()=>{scale=Math.min(1.3,Math.round((scale+.1)*10)/10);apply()});controls.append(smaller,larger,status);prose.before(controls);apply()})();
/* Mark the section in view without changing URLs or moving focus. */
(()=>{if(!('IntersectionObserver'in window))return;const links=[...document.querySelectorAll('.chapter-nav nav a')];const byId=new Map(links.map(a=>[decodeURIComponent(a.hash.slice(1)),a]));const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>a.boundingClientRect.top-b.boundingClientRect.top);if(!visible.length)return;for(const a of links)a.removeAttribute('aria-current');byId.get(visible[0].target.id)?.setAttribute('aria-current','location')},{rootMargin:'-8% 0px -60% 0px'});for(const id of byId.keys()){const h=document.getElementById(id);if(h)observer.observe(h)}})();
